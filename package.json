{
  "name": "@rello-platform/hecm-types",
  "version": "0.1.0",
  "description": "Canonical HECM (reverse-mortgage) compute contract — the input Milo's deterministic engine consumes and the recommendation it returns. Shared by Milo, Rello, and PathfinderPro.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rello-platform/hecm-types.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
